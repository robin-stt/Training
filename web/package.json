{
  "name": "dagsformen",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute dagsformen --remote --file=./schema.sql",
    "db:init-local": "wrangler d1 execute dagsformen --local --file=./schema.sql"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.75.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260831.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
